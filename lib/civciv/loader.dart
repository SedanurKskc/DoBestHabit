import 'package:flutter/material.dart';

class CivcivLoader extends StatelessWidget {
  const CivcivLoader({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: CircularProgressIndicator());
  }
}
